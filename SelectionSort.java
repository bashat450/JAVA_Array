package ArrayEx;
/**
public	class SelectionSort{
	//printing array
	public static void main(String args[]){
		//traverse through array n-1 5-1 = 4
		for(i=0;i<arr[i].length-1;i++){
		int smallest =i;
		for(int j=i+1;j<arr.length;j++){
			//checking smallest element with index
			if(arr[smallest]>arr[j]){
				smallest=j;
			}
		}
		//swap
			int temp=arr[smallest];
			arr[smallest]>arr[j];
			arr[i]=temp;
		}
		printarray(arr);
	}
}//selection sort
**/